//JSX - JavaScript XML
var template = (
    <div>
        <h1>Indecison App</h1>
        <p>This is some info.</p>
    </div>
);
var appRoot = document.getElementById("app");

ReactDOM.render(template, appRoot);